# Wallet SDK

The Wallet SDK provides utilities for interacting with multiple chains. It includes support for EVM networks, Solana, Stellar, Sui and others.

Usage examples can be found in the source under `packages/wallet-sdk/src` and the exported hooks and providers.
