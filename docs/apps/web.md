# Web Application

The `web` app is a Next.js project.

## Running Locally

```bash
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) after starting the server. Edits to `app/page.tsx` will reload automatically.
