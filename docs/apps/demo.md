# Demo Application

The `demo` app was bootstrapped with Create React App.

Common commands:

```bash
pnpm start    # start development server
pnpm test     # run tests
pnpm run build  # production build
```
